# AIOS Repository Rules

本文件定义 Codex 在 AIOS 仓库中的默认工作规则。用户当前指令和更高优先级的安全规则始终优先。

## 工作原则

1. **先理解再修改**：先阅读相关配置、注册表、工作流、调用关系和已有约定，再做最小范围修改。
2. **优先复用成熟 Skill**：先检查注册表和现有 Skill；成熟能力可满足需求时，不重复实现。引入外部 Skill 前必须执行试用与评估。
3. **复杂任务先规划**：涉及多个文件、多个阶段或行为变更时，先明确目标、边界、风险、任务顺序和验证方式。
4. **禁止未经验证宣称完成**：完成声明必须附实际验证证据。无法运行验证时，明确说明未验证项、原因和风险。
5. **保持四层边界**：Skill、Agent、Workflow、Knowledge 各自只承担 README 中定义的职责，禁止把完整流程、角色提示和经验记录混写为单一大提示词。
6. **最小改动**：不做任务范围外的重构、改名、格式化、依赖升级或 Phase 扩展。

## 知识沉淀规则

- 只记录经过代码、测试、运行结果、官方资料或明确决策验证的内容。
- 每条知识应说明来源、适用范围、验证日期和失效条件；推测必须明确标注，不得写成事实。
- 可复用技术模式写入 `knowledge/patterns/`；架构或产品取舍写入 `knowledge/decisions/`；已发生的失败案例写入 `knowledge/failures/`；复盘结论写入 `knowledge/lessons/`；外部研究写入 `knowledge/research/`。
- 不在知识库保存密码、Token、私钥、个人敏感信息或未经授权的内部数据。
- 新知识不得覆盖历史决策；变化应新增记录并链接被替代项。

## 文件命名规范

- 目录和普通文件使用小写 `kebab-case`，例如 `feature-development`、`skill-evaluation.md`。
- 约定入口文件使用大写名称：`README.md`、`AGENTS.md`、`WORKFLOW.md`、`SKILL.md`。
- YAML 文件使用 `.yaml` 扩展名，不使用 `.yml`。
- Knowledge 与 Spec 文件以 `YYYY-MM-DD-<topic>.md` 命名。
- 注册标识使用小写 `kebab-case`，创建后保持稳定；显示名称可独立调整。

## 修改与验证

- 修改配置或注册表时，同时检查引用方、标识唯一性、枚举值和 YAML 可解析性。
- 修改 Workflow 时，确保每一步都有输入、输出、进入条件和退出条件，并与注册表保持一致。
- 新增 Skill、Agent 或 Workflow 时，必须同步登记对应的 `registry/*.yaml`。
- 不得通过删除测试、放宽标准或隐藏错误来制造通过结果。
- 未经用户明确要求，不提交、不推送、不部署。
